# spring.application.name=shopping-cart

# server.port=9092

# spring.datasource.url=jdbc://mysql://localhost:3306/jumia
# spring.datasource.username=root
# spring.datasource.password=admin


# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true 

# # create,update,create-drop,validate
# spring.jpa.hibernate.ddl-auto=update

# api.prefix=/api/v1


spring.application.name=shopping-cart

server.port=9092

# Neon PostgreSQL DB connection
spring.datasource.url=jdbc:postgresql://ep-mute-lake-a8y46pek-pooler.eastus2.azure.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_z7DByUl0MHCx

# PostgreSQL driver
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate dialect for PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Hibernate configuration
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

# Custom API prefix (optional for your routing)
api.prefix=/api/v1

